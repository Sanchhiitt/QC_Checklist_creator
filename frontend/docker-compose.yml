version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4444:4444"
    environment:
      - VITE_API_URL=http://localhost:5555
    restart: always
