version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: qc-checklist-backend
    ports:
      - "6000:6000"
    env_file:
      - .env
    restart: always
