name: qc-checklist-backend
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: qc-checklist-backend
    container_name: qc-checklist-backend
    ports:
      - "6000:6000"
    env_file:
      - .env
    restart: always